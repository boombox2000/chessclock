#ifndef DEFAULTS_H
#define DEFAULTS_H

#include <Preferences.h>

//This shows you how to save user defaults to flash memory and retrieve it. 
//const char* KEY_CLOCK_INTERVAL = "clock_interval";

// // Open the preferences
// Preferences preferences;

// // Start a transaction to write to the preferences
// preferences.begin("my_app", false);

// // Write the clock interval value
// int clockInterval = 10;  // Example value
// preferences.putInt(KEY_CLOCK_INTERVAL, clockInterval);

// // End the transaction and save the changes to Flash memory
// preferences.end();

// // Open the preferences
// Preferences preferences;

// // Start a transaction to read from the preferences
// preferences.begin("my_app", true);

// // Read the clock interval value
// int clockInterval = preferences.getInt(KEY_CLOCK_INTERVAL, 10);  // 10 is the default value

// // End the transaction
// preferences.end();

#endif
